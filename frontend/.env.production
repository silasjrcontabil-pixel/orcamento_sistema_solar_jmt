# Usado automaticamente pelo Vite em `npm run build` (modo production) — aponta pro backend
# real no Render. Não precisa de barra final nem /api. Ver .env para o valor usado em dev local.
VITE_API_URL=https://orcamento-sistema-solar-jmt.onrender.com
